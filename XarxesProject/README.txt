Instructions:
 
In screen of Lobby Select tipe of socket (UDP or TCP)
Then select if you want to be the server or client
The client can write their nickname and the IP of they want to send message
After that you will able to send message to the server if they are running
 the programan on a local conexion(another pc of the same network or same pc with another instance of unity)

Unfortunattely if you select the server you can't be able to se if you are connect with anyone,
 but if you run the program with unity you can see the received messages in the log.
